# git-filter-repo 替换规则：将历史中的密钥替换为占位符，避免 GitHub Push Protection 拦截
#
# 步骤（在项目根目录执行）：
#   1. pip install git-filter-repo
#   2. git filter-repo --replace-text scripts/rewrite_history_remove_secrets.txt --force
#   3. git remote add origin <原 remote URL>   # filter-repo 会删掉 remote，需重新添加
#   4. git push --force-with-lease origin main
#
# 注意：会重写全部提交哈希；若多人协作需先沟通，其他人需重新 clone 或 rebase。

LTAI5tGnX6CUm2TJkHEkkJwU===>REDACTED_OSS_ACCESS_KEY_ID
GjtFbtjByylIffYwk19zveabsjw4Xa===>REDACTED_OSS_ACCESS_KEY_SECRET
