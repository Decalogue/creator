# 复制到 src/.env 并填入真实值；.env 已 gitignore，勿提交密钥。
# 需安装 python-dotenv 后，config 导入时才会把 .env 载入 os.environ：pip install python-dotenv

# CREATOR_FRONTEND_URL=http://localhost:5002
# CREATOR_BACKEND_URL=http://localhost:5200
# CREATOR_QDRANT_URL=http://localhost:6333/dashboard

# ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
# ARK_API_KEY=

# EVERMEMOS_API_KEY=

# Flask（api_flask.py）
# FLASK_DEBUG=0
# FLASK_SECRET_KEY=your-secret-key
# FLASK_HOST=0.0.0.0
# FLASK_PORT=5200

# /api/chat 默认模型与历史轮数
# CHAT_DEFAULT_MODEL=DeepSeek-v3-2
# CHAT_MESSAGES_MAX_HISTORY=10

# 小说创作默认模型（create/continue/polish/chat）
# NOVEL_LLM_MODEL=kimi-k2-5

# 创作任务队列上限、SSE 心跳间隔（秒）
# CREATOR_TASKS_MAX=200
# SSE_KEEPALIVE_SEC=30
