---
description: 安全与敏感信息（不提交密钥、配置用环境变量）
globs: []
alwaysApply: true
---
- 不提交 API Key、密码、token、生产 URL；配置用环境变量或 `.env`，且 `.env` 在 `.gitignore` 中。
- 示例与文档中不写真实密钥；用占位符如 `YOUR_API_KEY`、`$ENV_VAR`。
- 硬编码仅限本地/示例用（如 localhost、示例 project_id），生产相关一律从配置读取。
