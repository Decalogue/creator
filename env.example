***REMOVED*** UniMem 与 Neo4j 环境变量示例（复制为 .env 或 export 后启动后端）
***REMOVED*** 使用方式：export $(grep -v '^***REMOVED***' env.example | xargs) 或 source .env

***REMOVED*** ---------- UniMem 后端（写入 Qdrant / Neo4j） ----------
***REMOVED*** UNIMEM_GRAPH_BACKEND 默认 neo4j；其余不设则默认 memory
UNIMEM_STORAGE_BACKEND=redis
***REMOVED*** UNIMEM_GRAPH_BACKEND=neo4j
UNIMEM_VECTOR_BACKEND=qdrant

***REMOVED*** 可选：UniMem 首次初始化超时（秒），默认 30
***REMOVED*** UNIMEM_INIT_TIMEOUT=30

***REMOVED*** 可选：不设则不连 LightRAG（避免 localhost:9621）
***REMOVED*** UNIMEM_LIGHTRAG_URL=

***REMOVED*** ---------- Neo4j（LTM Memory 节点，需先启动 Neo4j） ----------
***REMOVED*** 端口按你实际配置（例如 7680）
NEO4J_URI=bolt://localhost:7680
NEO4J_USER=neo4j
NEO4J_PASSWORD=seeme_db
NEO4J_DATABASE=neo4j

***REMOVED*** ---------- Qdrant（向量库，需先启动 Qdrant） ----------
***REMOVED*** UNIMEM_QDRANT_HOST=localhost
***REMOVED*** UNIMEM_QDRANT_PORT=6333
***REMOVED*** UNIMEM_COLLECTION_NAME=unimem_memories
